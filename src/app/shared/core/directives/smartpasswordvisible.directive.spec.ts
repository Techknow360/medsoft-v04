import { SmartpasswordvisibleDirective } from './smartpasswordvisible.directive';

describe('SmartpasswordvisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new SmartpasswordvisibleDirective();
    expect(directive).toBeTruthy();
  });
});
