import { SavevalidationDirective } from './savevalidation.directive';

describe('SavevalidationDirective', () => {
  it('should create an instance', () => {
    const directive = new SavevalidationDirective();
    expect(directive).toBeTruthy();
  });
});
